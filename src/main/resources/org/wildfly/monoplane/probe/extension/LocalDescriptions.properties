probe=Monitor subsystem
probe.add=Adds the subsystem
probe.remove=Removes the subsystem

probe.server-monitor=A server monitor
probe.server-monitor.add=Adds a monitor
probe.server-monitor.remove=Removes a monitor
probe.server-monitor.enabled=Enable monitoring (requires reload/restart)
probe.server-monitor.num-threads=Thread pool size for the scheduler

probe.storage-adapter=The storage adapter to persist the data points
probe.storage-adapter.add=Adds a storage adapter
probe.storage-adapter.remove=Removes a  storage adapter
probe.storage-adapter.url=The url of the remote storage system
probe.storage-adapter.user=Access token of the storage system
probe.storage-adapter.password=Access token of the storage system
probe.storage-adapter.token=Access token of the storage system
probe.storage-adapter.name=Name of the storage adapter type (rhq|influx|mono)
probe.storage-adapter.db=Name of the database to write to

probe.server-monitor.data-input=Definition of a single resource attribute to monitor
probe.server-monitor.data-input.add=Adds a data input
probe.server-monitor.data-input.remove=Removes a data input
probe.server-monitor.data-input.resource=DMR path of the management resource
probe.server-monitor.data-input.name=(Display) name of the Metric
probe.server-monitor.data-input.attribute=Name of the desired attribute inside the management resource
probe.server-monitor.data-input.seconds=Schedule interval in seconds
probe.server-monitor.data-input.minutes=Schedule interval in minutes
probe.server-monitor.data-input.hours=Schedule interval in hours

probe.diagnostics=Subsystem diagnostics
probe.diagnostics.add=Adds a diagnostics element
probe.diagnostics.remove=Removes a diagnostics element
probe.diagnostics.enabled=Enables diagnostics reporting
probe.diagnostics.seconds=Report interval in seconds
probe.diagnostics.minutes=Report interval in minutes

